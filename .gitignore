node_modules/
out/
dist/
*.log
*.tsbuildinfo

.env
.env.local
config.json

# per-user download bookkeeping, not something to commit. The manifest lives
# inside the output folder, so ignore the default output folder itself.
downloads/
.beatmap-downloader-manifest.json
